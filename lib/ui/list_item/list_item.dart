import 'package:flutter/material.dart';

class UIListItem extends StatefulWidget {
  const UIListItem({Key? key}) : super(key: key);

  @override
  State<UIListItem> createState() => _UIListItemState();
}

class _UIListItemState extends State<UIListItem> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
